[project]
name = "codecrafters-redis"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["asyncio>=4.0.0"]

[dependency-groups]
dev = ["pytest>=9.0.3"]


[tool.pyright]
extraPaths = ["app"]
